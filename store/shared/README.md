# Shared Store Content

This folder contains store content that should stay aligned between Google Play
and the App Store whenever possible.

- `listing/en-US/`: shared English listing copy.
- `assets/`: shared generated store graphics and screenshots.
- `release/`: shared release notes.

Keep platform-specific policy answers, credentials, caches, and publishing
scripts in `store/play/` or `store/app_store/`.
